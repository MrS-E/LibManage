extends layout

block content
    .row
        .col-10
            input.form-control(placeholder="search" type="text" id="search")
        .col-2
            button.btn.btn-primary(id="search_btn") Search
    script(src='/public/javascript/liste.js')
